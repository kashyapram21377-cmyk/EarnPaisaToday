echo '{
  "hosting": {
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ]
  }
}' > firebase.json

git add firebase.json
git commit -m "Add firebase.json"
git push origin main
